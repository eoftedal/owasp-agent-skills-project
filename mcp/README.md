# MCP for security guidance for ASVS

## Format

ASVS 5.0 data is stored as JSON in src/data/

## Running

- http transport: `npm run stateless` (available on http://localhost:3000/mcp )
- http transport: `npm run stateful` (available on http://localhost:3000/mcp )
- stdio transport: `npm run stdio`
